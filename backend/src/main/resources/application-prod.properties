# Production overrides
# Activate with: SPRING_PROFILES_ACTIVE=prod

# Enforce secure auth cookie by default in production.
jwt.cookie.secure=${JWT_COOKIE_SECURE:true}

# Keep Lax by default; set to None for cross-site frontend/backend over HTTPS.
jwt.cookie.same-site=${JWT_COOKIE_SAME_SITE:Lax}
